# =============================================================================
# App - MySQL root secret template. Copy to .env.production.secrets and fill in.
# Never commit the filled-in file (already covered by .env.*.secrets in .gitignore).
# =============================================================================

MYSQL_ROOT_PASSWORD=YOUR_SECURE_PASSWORD_HERE
