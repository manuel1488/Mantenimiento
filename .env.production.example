# =============================================================================
# App - Production Environment Variables
# Copy .env.production.secrets.example to .env.production.secrets for
# MYSQL_ROOT_PASSWORD — pass both files:
#   --env-file .env.production --env-file .env.production.secrets
# =============================================================================

# Container Configuration
CONTAINER_PREFIX=app-prod
BUILD_CONFIGURATION=Release

# Application Configuration
Application__Version=1.0.0
Application__DefaultLanguage=es-MX
Application__SupportedLanguages__0=es-MX
Application__SupportedLanguages__1=en-US
Application__BaseUrl=https://your-domain.com

# Brand identity — must match a file in src/App.Web/Branding/{profile}.json
# (app name, logo path, theme colors). Add a new Branding/{tenant}.json to onboard a tenant.
BRANDING_PROFILE=cleeny

# Server Configuration
PORT=8080
TZ=America/Mexico_City

# Resource Limits
CPU_LIMIT=1.0
MEMORY_LIMIT=1G
CPU_RESERVATION=0.5
MEMORY_RESERVATION=512M

# MySQL Configuration (if using containerized DB)
# MYSQL_ROOT_PASSWORD lives in .env.production.secrets
MYSQL_DATABASE=YOUR_DATABASE_NAME_HERE
MYSQL_USER=YOUR_DB_USER_HERE
MYSQL_PASSWORD=YOUR_SECURE_PASSWORD_HERE
MYSQL_PORT=3306

# MySQL Resource Limits
DB_CPU_LIMIT=2.0
DB_MEMORY_LIMIT=2G
DB_CPU_RESERVATION=1
DB_MEMORY_RESERVATION=1G

# Database Configuration (Production)
DATABASE_CONNECTION_STRING=Server=db;Database=YOUR_DATABASE_NAME_HERE;User=YOUR_DB_USER_HERE;Password=YOUR_SECURE_PASSWORD_HERE;
