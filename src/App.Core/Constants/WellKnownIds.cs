namespace App.Core.Constants;

public static class WellKnownIds
{
    /// <summary>Customer Id reserved for "Público General" — seeded at startup, never reassigned.</summary>
    public const long PublicGeneralCustomerId = 1;
}
