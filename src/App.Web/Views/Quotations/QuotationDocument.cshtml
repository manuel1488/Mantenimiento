@using App.Core.DTOs.Shop
@using App.Services.Shop
@using Microsoft.Extensions.Localization
@using App.Shared.Services
@inject IStringLocalizer<QuotationService> L
@inject IDateTime _dateTime
@model QuotationPdfDto

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>@L["Quotation"] @Model.QuotationNumber</title>
    <style>
        :root {
            --primary-color: #1A6868;
            --primary-light: #E0F2F1;
            --secondary-color: #546E7A;
        }

        body {
            font-family: 'Roboto', Arial, sans-serif;
            margin: 0;
            padding: 24px;
            color: #263238;
            font-size: 13px;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 24px;
            border-bottom: 3px solid var(--primary-color);
            padding-bottom: 16px;
        }

        .logo {
            max-width: 160px;
            max-height: 60px;
        }

        .company-info { text-align: right; }

        .company-name {
            font-size: 20px;
            font-weight: bold;
            color: var(--primary-color);
        }

        .company-detail {
            font-size: 12px;
            color: var(--secondary-color);
            margin-top: 2px;
        }

        .quotation-title {
            font-size: 22px;
            font-weight: bold;
            color: var(--primary-color);
            margin-bottom: 16px;
        }

        .meta-grid {
            display: flex;
            gap: 32px;
            margin-bottom: 20px;
        }

        .meta-block { flex: 1; }

        .meta-label {
            font-size: 10px;
            color: var(--secondary-color);
            text-transform: uppercase;
            letter-spacing: 0.6px;
            font-weight: 500;
        }

        .meta-value {
            font-size: 14px;
            font-weight: 500;
            margin-top: 2px;
            color: #263238;
        }

        .validity-badge {
            display: inline-block;
            background: var(--primary-light);
            color: var(--primary-color);
            border: 1px solid var(--primary-color);
            border-radius: 4px;
            padding: 2px 10px;
            font-size: 13px;
            font-weight: 600;
        }

        .info-row {
            display: flex;
            gap: 16px;
            margin-bottom: 20px;
        }

        .customer-section {
            flex: 1;
            background: #F5F5F5;
            border-radius: 8px;
            padding: 12px 16px;
        }

        .fiscal-section {
            flex: 1;
            background: var(--primary-light);
            border-radius: 8px;
            padding: 12px 16px;
            border-left: 4px solid var(--primary-color);
        }

        .section-title {
            font-size: 10px;
            color: var(--secondary-color);
            text-transform: uppercase;
            letter-spacing: 0.6px;
            font-weight: 500;
            margin-bottom: 6px;
        }

        .customer-name { font-size: 15px; font-weight: 600; color: #263238; }
        .customer-legal { font-size: 12px; color: var(--secondary-color); margin-top: 1px; }

        .customer-detail {
            font-size: 11px;
            color: #546E7A;
            margin-top: 3px;
        }

        .fiscal-row {
            display: flex;
            gap: 24px;
            margin-top: 4px;
        }

        .fiscal-item { }
        .fiscal-item-label {
            font-size: 10px;
            color: var(--secondary-color);
            text-transform: uppercase;
            letter-spacing: 0.4px;
        }
        .fiscal-item-value {
            font-size: 13px;
            font-weight: 600;
            color: var(--primary-color);
            margin-top: 1px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 16px;
        }

        thead tr { background: var(--primary-color); color: white; }

        thead th {
            padding: 9px 12px;
            text-align: left;
            font-size: 11px;
            font-weight: 600;
            letter-spacing: 0.3px;
        }

        thead th.text-right { text-align: right; }

        tbody tr:nth-child(even) { background: #F8FAFB; }

        tbody td {
            padding: 8px 12px;
            border-bottom: 1px solid #ECEFF1;
            font-size: 12px;
        }

        tbody td.text-right { text-align: right; }

        .totals-section {
            display: flex;
            justify-content: flex-end;
            margin-bottom: 20px;
        }

        .totals-table { width: 280px; }

        .totals-table td {
            padding: 5px 12px;
            border: none;
        }

        .totals-table .label {
            color: var(--secondary-color);
            font-size: 12px;
        }

        .totals-table .value { text-align: right; font-size: 13px; }

        .totals-table .total-row {
            border-top: 2px solid var(--primary-color);
        }

        .totals-table .total-row td {
            font-size: 15px;
            font-weight: bold;
            color: var(--primary-color);
            padding-top: 8px;
        }

        .notes-section {
            background: #F5F5F5;
            border-left: 4px solid var(--primary-color);
            border-radius: 4px;
            padding: 12px 16px;
            margin-bottom: 20px;
        }

        .notes-title {
            font-size: 10px;
            color: var(--secondary-color);
            text-transform: uppercase;
            letter-spacing: 0.6px;
            font-weight: 500;
            margin-bottom: 4px;
        }

        .notes-text {
            font-size: 12px;
            line-height: 1.5;
            white-space: pre-wrap;
            color: #37474F;
        }

        .footer {
            text-align: center;
            font-size: 11px;
            color: var(--secondary-color);
            border-top: 1px solid #CFD8DC;
            padding-top: 12px;
            margin-top: 8px;
        }
    </style>
</head>
<body>

    <div class="header">
        <div>
            @if (!string.IsNullOrEmpty(Model.LogoBase64))
            {
                <img src="@Model.LogoBase64" class="logo" alt="Logo" />
            }
        </div>
        <div class="company-info">
            <div class="company-name">@Model.CompanyName</div>
        </div>
    </div>

    <div class="quotation-title">@L["Quotation"] — @Model.QuotationNumber</div>

    <div class="meta-grid">
        <div class="meta-block">
            <div class="meta-label">@L["Date"]</div>
            <div class="meta-value">@Model.QuoteDate.ToString("dd/MM/yyyy")</div>
        </div>
        <div class="meta-block">
            <div class="meta-label">@L["Valid Until"]</div>
            <div class="meta-value">
                <span class="validity-badge">@Model.ValidUntil.ToString("dd/MM/yyyy")</span>
            </div>
        </div>
        <div class="meta-block">
            <div class="meta-label">@L["Quotation #"]</div>
            <div class="meta-value">@Model.QuotationNumber</div>
        </div>
    </div>

    <div class="info-row">
        @* Customer block *@
        <div class="customer-section">
            <div class="section-title">@L["Customer"]</div>
            <div class="customer-name">@Model.CustomerName</div>
            @if (!string.IsNullOrEmpty(Model.CustomerLegalName) && Model.CustomerLegalName != Model.CustomerName)
            {
                <div class="customer-legal">@Model.CustomerLegalName</div>
            }
            @if (!string.IsNullOrEmpty(Model.CustomerEmail))
            {
                <div class="customer-detail">✉ @Model.CustomerEmail</div>
            }
            @if (!string.IsNullOrEmpty(Model.CustomerPhone))
            {
                <div class="customer-detail">☎ @Model.CustomerPhone</div>
            }
            @if (!string.IsNullOrEmpty(Model.CustomerAddress))
            {
                <div class="customer-detail">@Model.CustomerAddress</div>
            }
        </div>

        @* Fiscal block — only shown when customer has fiscal data *@
        @if (Model.CustomerHasFiscalData && !string.IsNullOrEmpty(Model.CustomerTaxId))
        {
            <div class="fiscal-section">
                <div class="section-title">@L["Fiscal Data"]</div>
                <div class="fiscal-row">
                    @if (!string.IsNullOrEmpty(Model.CustomerTaxId))
                    {
                        <div class="fiscal-item">
                            <div class="fiscal-item-label">@L["Tax ID"]</div>
                            <div class="fiscal-item-value">@Model.CustomerTaxId</div>
                        </div>
                    }
                    @if (!string.IsNullOrEmpty(Model.CustomerFiscalRegime))
                    {
                        <div class="fiscal-item">
                            <div class="fiscal-item-label">@L["Fiscal Regime"]</div>
                            <div class="fiscal-item-value">@Model.CustomerFiscalRegime</div>
                        </div>
                    }
                </div>
            </div>
        }
    </div>

    <table>
        <thead>
            <tr>
                <th>#</th>
                <th>@L["Code"]</th>
                <th>@L["Product"]</th>
                <th class="text-right">@L["Qty"]</th>
                <th class="text-right">@L["Unit Price"]</th>
                @if (Model.Details.Any(d => d.DiscountPercentage > 0))
                {
                    <th class="text-right">@L["Discount"]</th>
                }
                <th class="text-right">@L["Tax"]</th>
                <th class="text-right">@L["Total"]</th>
            </tr>
        </thead>
        <tbody>
            @for (int i = 0; i < Model.Details.Count; i++)
            {
                var item = Model.Details[i];
                <tr>
                    <td>@(i + 1)</td>
                    <td>@item.ProductCode</td>
                    <td>@item.ProductName</td>
                    <td class="text-right">@item.Quantity.ToString("G29")</td>
                    <td class="text-right">@Model.CurrencySymbol@item.UnitPrice.ToString("N2")</td>
                    @if (Model.Details.Any(d => d.DiscountPercentage > 0))
                    {
                        <td class="text-right">
                            @if (item.DiscountPercentage > 0)
                            {
                                @($"{item.DiscountPercentage:N1}%")
                            }
                        </td>
                    }
                    <td class="text-right">@Model.CurrencySymbol@item.TaxAmount.ToString("N2")</td>
                    <td class="text-right">@Model.CurrencySymbol@item.Total.ToString("N2")</td>
                </tr>
            }
        </tbody>
    </table>

    <div class="totals-section">
        <table class="totals-table">
            <tr>
                <td class="label">@L["Subtotal"]</td>
                <td class="value">@Model.CurrencySymbol@Model.Subtotal.ToString("N2")</td>
            </tr>
            @if (Model.DiscountAmount > 0)
            {
                <tr>
                    <td class="label">@L["Discount"]</td>
                    <td class="value">- @Model.CurrencySymbol@Model.DiscountAmount.ToString("N2")</td>
                </tr>
            }
            <tr>
                <td class="label">@L["Tax"]</td>
                <td class="value">@Model.CurrencySymbol@Model.TaxAmount.ToString("N2")</td>
            </tr>
            <tr class="total-row">
                <td class="label">@L["Total"]</td>
                <td class="value">@Model.CurrencySymbol@Model.Total.ToString("N2")</td>
            </tr>
        </table>
    </div>

    @if (!string.IsNullOrEmpty(Model.Notes))
    {
        <div class="notes-section">
            <div class="notes-title">@L["Notes &amp; Conditions"]</div>
            <div class="notes-text">@Model.Notes</div>
        </div>
    }

    <div class="footer">
        @Model.CompanyName — @L["This quotation is valid until"] @Model.ValidUntil.ToString("dd/MM/yyyy")
    </div>
</body>
</html>
